{
    "$schema": "http://json.schemastore.org/proxies",
    "proxies": {      
      "proxy": {
        "matchCondition": {
          "methods": [ "GET" ],
          "route": "/broadcast/{*restOfPath}"
        },
        "backendUri": "{restOfPath}"
      }
    }
  }